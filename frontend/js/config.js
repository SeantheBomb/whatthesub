// After first deploy, update API_BASE with your workers.dev URL.
// Run `npm run deploy:api` and copy the URL from the output.
// Example: 'https://whatthesub-api.seanf.workers.dev'
//
// For local dev: run `npm run dev:api` (starts on :8787) and set to 'http://localhost:8787'
const CONFIG = {
  API_BASE: 'https://whatthesub-api.sean-feeser.workers.dev',
};
